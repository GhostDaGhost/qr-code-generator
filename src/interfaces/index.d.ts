interface LinkInputProps {
    setQRCodeLink: any;
    isLoading: boolean;
}

interface GeneratedQRCodeProps {
    generatedQRCodeLink: string;
}

interface ErrorAlertProps {
    errorMessage: string;
}
